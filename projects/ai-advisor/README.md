# AI Advisor

Strata semantic-view project.

- `base/*.sql` — base views (deployed as `CREATE OR REPLACE VIEW`).
- `semantic_views/*.yaml` — native Snowflake semantic views.

Create a feature branch, edit, **Test**, open a merge request, and merge.
